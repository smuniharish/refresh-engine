# Optional Codex/ChatGPT skill presentation metadata.
# Reference: https://learn.chatgpt.com/docs/build-skills ("Optional metadata").
# This file is read only by Codex CLI / the ChatGPT desktop app when this skill
# directory is discovered under an .agents/skills location (see repository
# root README for the exact discovery path used in this repo). It is not part
# of the refresh_engine Python package and has no effect on the package itself.
interface:
  display_name: "refresh-engine"
  short_description: "Integrate, extend, and debug the refresh-engine Python refresh/change-detection library."
  default_prompt: "Help me integrate refresh-engine for periodic, incremental, dependency-aware resource refresh."
policy:
  allow_implicit_invocation: true
